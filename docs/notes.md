## Curent Development

The kernel is currently developed and tested using release builds.

Debug builds may include additional runtime checks from `core` that are not yet suitable during early bare-metal bring-up.